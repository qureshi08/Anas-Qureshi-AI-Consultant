# Copy to .env.local for local dev. Set the SAME variables in Vercel:
# Project -> Settings -> Environment Variables.

NEXT_PUBLIC_SUPABASE_URL=https://hidqqawjhxafcavrjopg.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# Server-only secret (full database access). NEVER commit it, NEVER expose it to
# the browser. Set it in .env.local locally and in Vercel env vars only.
SUPABASE_SERVICE_ROLE=your-service-role-secret

# AI assistant (Groq, free tier, works globally, no card). Get a free key at
# https://console.groq.com/keys . Server-only. Set it in Vercel env vars.
GROQ_API_KEY=your-groq-api-key

# Instant lead notifications (Resend, free tier, no card, no domain needed).
# Get a free key at https://resend.com/api-keys . Server-only, set in Vercel.
# Emails go to NOTIFY_EMAIL (defaults to muhammadanasq@gmail.com if unset).
RESEND_API_KEY=your-resend-api-key
NOTIFY_EMAIL=muhammadanasq@gmail.com
